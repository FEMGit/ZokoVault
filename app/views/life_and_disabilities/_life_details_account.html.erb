<% @insurance_card.policy.each do |policy| %>
  <div>
    <h1><%= "#{policy.policy_type} -  #{policy.policy_holder && policy.policy_holder.name}" %></h1>
    <div class="card <%= @insurance_card.group %> details insurance">
      <%= link_to content_tag(:a), "#{@insurance_card.id}/edit", :class => "whole-card-link", method: :get%>
      <div class="details-wrapper">
        <div class="details-card-info details">
          <h2 class="provider-details-title">Policy Information</h2>
            <div class = "details-card-field">
            <label class="title">Coverage Amount</label>
            <span class="details-field details-field-value"><%= policy.coverage_amount && number_to_currency(policy.coverage_amount, unit: "$", precision: 0) %></span>
          </div>
          <div class = "details-card-field">
            <label class="title">Policy Number</label>
            <span class="details-field details-field-value"><%= policy.policy_number %></span>
          </div>
          <div class = "details-card-field">
            <label class="title">Primary Beneficiaries</label>
            <% policy.primary_beneficiaries.each do |primary| %>
              <span class="details-field details-field-value"><%= primary && primary.name %></span>
            <% end %>
          </div>
          <div class = "details-card-field">
            <label class="title">Secondary Beneficiaries</label>
            <% policy.secondary_beneficiaries.each do |secondary| %>
              <span class="details-field details-field-value"><%= secondary && secondary.name %></span>
            <% end %>
          </div>
          <div class = "details-card-field">
            <label class="title">Primary contact</label>
            <span class="details-field details-field-value"><%= policy.broker_or_primary_contact && policy.broker_or_primary_contact.name %></span>
          </div>
          <div class = "details-card-field details-notes">
            <label class="title">Notes</label>
            <span class="details-field details-field-value">
             <%= policy.notes %>
            </span>
          </div>
        </div>
      </div>
    </div>
  </div>
<% end %>