Hello Files!
  <%# @files.each do |file| %>
    <%#= link_to file do %>
    <img src="<%#= file.name.url %>">
    <%# end %>
  <%# end %>

<br>

<%= link_to 'New file', new_vault_file_path %>

<h1>New file</h1>

<%= form_for(@file) do |f| %>
  <div>
    <%= f.label :name %><br>
    <%= f.file_field :name %>
  </div>
  <div>
    <%= f.submit %>
  </div>
<% end %>

<%= link_to 'Back', files_path %>

<h1>New file</h1>

<%= form_for(@file) do |f| %>
  <div>
    <%= f.label :name %><br>
    <%= f.file_field :name %>
  </div>
  <div>
    <%= f.submit %>
  </div>
<% end %>

<%= link_to 'Back', files_path %>

<%= link_to 'Destroy', @file,  method: :delete, data: { confirm: 'Are you sure?' } %>
<img src="<%= @file.name.url %>" /><br/>


<%= link_to 'Destroy', @file,  method: :delete, data: { confirm: 'Are you sure?' } %>
<img src="<%= @file.name.url %>" /><br/>
