<%= content_for :title, "Health" %>
<%= content_for :add_another_title, "Policy" %>
<%= content_for :partial_path, "healths/health_add_account" %>
<%= content_for :url_path, "#{health_path}" %>
<%= content_for :delete_path, "#{healths_path}/" %>
<%= render partial: "layouts/new_insurance_account" %>
