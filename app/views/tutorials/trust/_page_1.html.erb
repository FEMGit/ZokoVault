 <button class="primary-button big-button hidden unsaved-changes-modal" data-modal-id="wizard-modal"></button>
 <div id="wizard-modal" class="modal wizard-modal modal-no-js confirmation-modal">
  <div class="confirmation-wrapper">
    <h2>Unsaved changes</h2>
    <p>These items will be permanently deleted and cannot be recovered. Are you sure?</p>
  </div>
  <footer class="modal-footer confirmation-footer">
    <%= link_to 'Save and Continue', '#', class: "button primary-button big-button save-and-continue" %>
    <%= link_to 'Skip and Continue', '#', class: "button primary-button big-button skip-and-continue" %>
  </footer>
</div>

<div class="card mb-30 mt-20 max-w-1000-centered">
  <h1 class="mb-20">Add your Trust</h1>

  <%- @trust = Trust.new(user: resource_owner, category: Category.fetch(Rails.application.config.x.TrustsEntitiesCategory.downcase)) %>

  <%= form_for @trust, url: url_for(action: :create, controller: :trusts) do |f| %>
    <%= hidden_field_tag :next_page_number, @next_page %>
    <%= hidden_field_tag :page_number, @page_number %>

    <%= label_tag "trust_name", 'Trust Name' %>
    <%= render partial: 'tutorials/trust/trusts' %>

    <div class="collect-fields">
      <%= hidden_field_tag :tutorial_name, @tutorial_name %>
      <%= hidden_field_tag :next_tutorial, @next_tutorial.try(:name).try(:parameterize) || 'confirmation_page' %>
      <div class="input-pair tutorial-fields trust-fields">
        <%= text_field_tag "vault_entry_0[name]", "", class: "form-control medium repeated-field", placeholder: 'Trust Name', :maxlength => get_max_length(:wtl_name) %>
        <a class="medium-button button primary-button inline-button add-another-btn">Add Another</a>
      </div>
    </div>

    <%= render partial: 'tutorials/buttons_section', locals: { f: f } %>
  <% end %>
</div>
