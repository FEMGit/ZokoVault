<%= render :partial => "layouts/bread_crumbs" %>

<% entity = {name: 'Entity 1', partners: [ {name: 'Homer Simpson'}, {name: 'Marge Simpson'}] , advisors: [{name: 'Bill Azaria'}],
shares_initials: ['JS', 'BB', 'PS'],
notes: 'Notes notes notes notes notes notes notes notes notes notes notes notes notes'} %>

<%= link_to edit_entity_path, class: "edit-link mrg-top" do%>
  <span class="circle-button">
    <svg class="icon circle-icon">
      <use xlink:href="#icon-pencil-1"></use>
    </svg>
  </span>Edit Entity
<% end %>
<h1><%= entity[:name] %></h1>

<div class="card cg-3 mb-20">
  <h3 class="card-label">Entity Details</h3>
  
  <label class="title">Owners/Partners</label>
  <p>
    <% entity[:partners].each do |partner| %>
      <%= partner[:name] %><br>
    <% end %>
  </p>

  <label class="title">Advisor</label>
  <p>
    <% entity[:advisors].each do |advisor| %>
      <%= advisor[:name] %><br>
    <% end %>
  </p>
  
  <label>Notes</label>
  <p><%= entity[:notes] %></p>
  <span class="clear"></span>
  
  <section class="shared-with">
    <h3 class="card-label shared-label">
      <svg class="icon" viewBox="0 0 20 20">
        <use xlink:href="#icon-Safe"></use>
      </svg> Shared With
    </h3>

    <label>Shared With</label>
    <section class="shared-list">
      <% entity[:shares_initials].each do |share_initials| %>
        <div class="tooltip-item">
          <p class="avatar-initials"><%= share_initials %></p>
          <%= image_tag(empty_image, class: "avatar-small") %>
        </div>
      <% end %>
    </section>
  </section>
</div>

<h3>Documents</h3>
<a class="add-link fr" href="#">
  <span class="circle-button">
    <svg class="icon circle-icon">
      <use xlink:href="#icon-plus-1"></use>
    </svg>
  </span>Add Document
</a>

<%= render :partial => "layouts/standard_document_table", :locals => {:documents => [] } %>