<!DOCTYPE html>
<html>
  <%= render partial: 'layouts/application_head_tag' %>
  
  <span class="bold-title">Shared View:</span><span class="user-name"> <%= current_user.name %> </span>
  <%= link_to "Exit", shared_path,
    class: "js-modal-close button primary-button big-button exit-button" %>
  

  <% if user_signed_in? %>
    <body class="logged-in">
  <% else %>
    <body class="logged-out">
  <% end %>

  <%= render :partial => 'layouts/svgdefs' %>
  <%= render :partial => 'layouts/alerts' %>
  <% if user_signed_in? %>
    <%= render :partial => 'layouts/shared_view_nav' %>
    <main class="app-nav">

    <% else %>
      <main class="marketing">

  <% end %>
  <%= render :partial => 'layouts/shared_view_header' %>

  <div class="content">
    <%= yield %>
  </div>

  <%= render :partial => 'layouts/footer' %>
  </main>

  </body>
</html>
