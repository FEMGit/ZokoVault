<%= link_to 'Destroy', @upload,  method: :delete, data: { confirm: 'Are you sure?' } %>
<img src="<%= @upload.url %>" /><br/>
<svg class="icon" x=2000>
  <use xlink:href="#icon-document-3"></use>
</svg>
